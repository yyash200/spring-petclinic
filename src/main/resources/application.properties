server.port=8081

# database init, supports mysql too
#database=h2
#spring.sql.init.schema-locations=classpath*:db/${database}/schema.sql
#spring.sql.init.data-locations=classpath*:db/${database}/data.sql
#spring.datasource.url = jdbc:mysql://localhost:3306/codefest2022
#spring.datasource.username = root
#spring.datasource.password =
        
spring.datasource.url = jdbc:mysql://sjn-spring-petclinic.mysql.database.azure.com:3306/codefest2022?useSSL=true&requireSSL=false
spring.datasource.username = root123@sjn-spring-petclinic
spring.datasource.password = Codefest@2022

# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always

# Web
spring.thymeleaf.mode=HTML

# JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=true

# Internationalization
spring.messages.basename=messages/messages

# Actuator
management.endpoints.web.exposure.include=*

# Logging
logging.level.org.springframework=INFO
# logging.level.org.springframework.web=DEBUG
# logging.level.org.springframework.context.annotation=TRACE

# Maximum time static resources should be cached
spring.web.resources.cache.cachecontrol.max-age=12h
